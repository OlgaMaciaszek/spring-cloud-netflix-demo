spring:
  application:
    name: user-service


eureka:
  client:
    healthcheck:
      enabled: true
logging:
  level:
    org.springframework: debug
server:
  port: 9082

management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream