spring:
  application:
    name: card-service
  cloud:
    loadbalancer:
      default: true
logging:
  level:
    org.springframework: info
server:
  port: 9080
management:
  endpoints:
    web:
      exposure:
        include:
          - metrics
          - prometheus