server:
  port: 9081

spring:
  application:
    name: fraud-verifier
eureka:
  client:
    healthcheck:
      enabled: true
logging:
  level:
    org.springframework: info
management:
  endpoints:
    web:
      exposure:
        include:
          - metrics
          - prometheus