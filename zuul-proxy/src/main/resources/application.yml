server:
  port: 9083
spring:
  application:
    name: proxy
logging:
  level:
    org.springframework: info

management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream
#hystrix:
#  command:
#    default:
#      circuitBreaker:
#        enabled: false

zuul:
  ignoredServices: 'ignored'
  routes:
    allowed:
      serviceId: ignored
      path: /**/test/allowed